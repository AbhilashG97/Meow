#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() 
{   
    char regex[] = {"\0"}, input[] = {"\0"};
    scanf("%s", regex);
    scanf("%s", input);

    int frequency_array[]

    for (int i=0; i<strlen(regex); i++) 
    {
        
    }

    // printf("%s %s", input, regex);
    return 0;
}